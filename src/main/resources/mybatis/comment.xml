<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="comment">

	
	
	<insert id="add">
		INSERT INTO partner_comments (id, comment_text, reg_date, write_id, member_id)
		VALUES (SEQ.PARTNER_COMMENTS_SEQ, #{commentText}, SYSDATE, #{writeId}, #{memberId})
	</insert>
	
</mapper>