<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="WriteAttachThumbnail2">
	
	<insert id="add">
	Insert into Attach_Thumbnail_write2 values(id_seq.NEXTVAL,#{filename})
	</insert>
	
	<select id="list" resultType="String">
		select filename from Attach_Thumbnail_write2 where id=56
	</select>
</mapper>